<Window x:Class="PhanVanLocWPF.BookingDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Booking Details - FUMiniHotelSystem"
        Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="ðŸ“…"
                           FontSize="24"
                           Foreground="White"
                           Margin="0,0,10,0"
                           VerticalAlignment="Center"/>
                <TextBlock Text="Booking Details"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Border Grid.Row="1" Background="White" Margin="20" CornerRadius="5" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Booking Info -->
                <Border Grid.Row="0" Background="#F8F9FA" CornerRadius="5" Padding="15" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Booking ID:" FontWeight="Bold" Margin="0,0,10,10"/>
                        <TextBlock x:Name="txtBookingID" Grid.Row="0" Grid.Column="1" Margin="0,0,20,10"/>

                        <TextBlock Grid.Row="0" Grid.Column="2" Text="Booking Date:" FontWeight="Bold" Margin="0,0,10,10"/>
                        <TextBlock x:Name="txtBookingDate" Grid.Row="0" Grid.Column="3" Margin="0,0,0,10"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Customer:" FontWeight="Bold" Margin="0,0,10,0"/>
                        <TextBlock x:Name="txtCustomer" Grid.Row="1" Grid.Column="1" Margin="0,0,20,0"/>

                        <TextBlock Grid.Row="1" Grid.Column="2" Text="Total Price:" FontWeight="Bold" Margin="0,0,10,0"/>
                        <TextBlock x:Name="txtTotalPrice" Grid.Row="1" Grid.Column="3" FontWeight="Bold" Foreground="#2196F3"/>
                    </Grid>
                </Border>

                <!-- Booking Details -->
                <DataGrid x:Name="dgBookingDetails" Grid.Row="1"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          RowHeaderWidth="0"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#F5F5F5">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#E3F2FD"/>
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="8"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Room" Binding="{Binding RoomInformation.RoomNumber}" Width="120"/>
                        <DataGridTextColumn Header="Room Type" Binding="{Binding RoomInformation.RoomType.RoomTypeName, TargetNullValue='N/A'}" Width="*"/>
                        <DataGridTextColumn Header="Check-in" Binding="{Binding StartDate, StringFormat=d}" Width="120"/>
                        <DataGridTextColumn Header="Check-out" Binding="{Binding EndDate, StringFormat=d}" Width="120"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding ActualPrice, StringFormat=C0}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#F0F0F0" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Close" 
                        Background="#9E9E9E" 
                        Foreground="White"
                        Padding="15,5"
                        Click="CloseButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

