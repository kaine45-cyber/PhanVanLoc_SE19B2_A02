<Window x:Class="PhanVanLocWPF.RoomEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Add/Edit Room"
        Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15">
            <TextBlock Text="Room Information" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="White"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Form -->
        <Border Grid.Row="1" Background="White" Margin="20" CornerRadius="5" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Room Number -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Room Number:" VerticalAlignment="Center" Margin="0,0,10,15" FontWeight="Bold"/>
                <TextBox x:Name="txtRoomNumber" Grid.Row="0" Grid.Column="1" Margin="0,0,0,15" Height="30" Padding="8,5"/>

                <!-- Room Type -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Room Type:" VerticalAlignment="Center" Margin="0,0,10,15" FontWeight="Bold"/>
                <ComboBox x:Name="cbRoomType" Grid.Row="1" Grid.Column="1" Margin="0,0,0,15" Height="30" Padding="8,5"
                          DisplayMemberPath="RoomTypeName" SelectedValuePath="RoomTypeID"/>

                <!-- Room Description -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Description:" VerticalAlignment="Top" Margin="0,10,10,15" FontWeight="Bold"/>
                <TextBox x:Name="txtRoomDescription" Grid.Row="2" Grid.Column="1" Margin="0,0,0,15" 
                         Height="60" TextWrapping="Wrap" AcceptsReturn="True" Padding="8,5"/>

                <!-- Max Capacity -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Capacity:" VerticalAlignment="Center" Margin="0,0,10,15" FontWeight="Bold"/>
                <TextBox x:Name="txtMaxCapacity" Grid.Row="3" Grid.Column="1" Margin="0,0,0,15" Height="30" Padding="8,5"/>

                <!-- Price Per Day -->
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Price/Day:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                <TextBox x:Name="txtPricePerDay" Grid.Row="4" Grid.Column="1" Height="30" Padding="8,5"/>
            </Grid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#F0F0F0" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save" 
                        Background="#4CAF50" 
                        Foreground="White"
                        Padding="15,8"
                        Margin="0,0,10,0"
                        Click="SaveButton_Click"/>
                <Button Content="Cancel" 
                        Background="#9E9E9E" 
                        Foreground="White"
                        Padding="15,8"
                        Click="CancelButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

