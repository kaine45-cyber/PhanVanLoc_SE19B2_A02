<Window x:Class="PhanVanLocWPF.CustomerBookingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="New Booking - FUMiniHotelSystem"
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="CalendarPlus" Width="32" Height="32" Foreground="White"/>
                <TextBlock Text="New Booking"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="White"
                           Margin="10,0,0,0"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Border Grid.Row="1" Background="White" Margin="20" CornerRadius="5" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Search and Filter -->
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox Grid.Column="0" 
                             x:Name="txtSearch" 
                             Height="30" 
                             Padding="10,5" 
                             VerticalAlignment="Center"
                             Margin="0,0,10,0"
                             materialDesign:HintAssist.Hint="Search rooms..."
                             materialDesign:HintAssist.IsFloating="True"/>
                    
                    <ComboBox Grid.Column="1" 
                              x:Name="cbRoomType" 
                              Height="30" 
                              Width="150" 
                              Margin="0,0,10,0"
                              DisplayMemberPath="RoomTypeName" 
                              SelectedValuePath="RoomTypeID"/>
                    
                    <Button Grid.Column="2" 
                           Content="Search" 
                           Background="#2196F3" 
                           Foreground="White" 
                           Padding="15,5" 
                           Click="SearchButton_Click"/>
                </Grid>

                <!-- Available Rooms -->
                <DataGrid x:Name="dgAvailableRooms" Grid.Row="1"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          RowHeaderWidth="0"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#F5F5F5">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#E3F2FD"/>
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="8"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Room Number" Binding="{Binding RoomNumber}" Width="120"/>
                        <DataGridTextColumn Header="Room Type" Binding="{Binding RoomType.RoomTypeName}" Width="*"/>
                        <DataGridTextColumn Header="Capacity" Binding="{Binding RoomMaxCapacity}" Width="100"/>
                        <DataGridTextColumn Header="Price/Day" Binding="{Binding RoomPricePerDay, StringFormat=C0}" Width="120"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding RoomDetailDescription}" Width="*"/>
                        <DataGridTemplateColumn Header="Actions" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Book" 
                                            Background="#4CAF50" 
                                            Foreground="White"
                                            Padding="8,4"
                                            Click="BookRoom_Click"
                                            Tag="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#F0F0F0" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Close" 
                        Background="#9E9E9E" 
                        Foreground="White" 
                        Padding="15,5" 
                        Click="CloseButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

